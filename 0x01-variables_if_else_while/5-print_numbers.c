#include <stdio.h>
/**
 * main - Entry point
 *print all single digit numbers
 * Return: Always 0 (Success)
 */
int main(void)
{
int a = 0;
for (; a < 10 ; a++)
{
printf("%d", a);
}
printf("\n");
return (0);
}
