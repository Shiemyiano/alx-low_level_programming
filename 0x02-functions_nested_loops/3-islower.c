/**
 * _islower - check if char is lower or not
 * @c: char to check
 * Return: if lower 1.
 * Otherwise, 0 is returned.
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
